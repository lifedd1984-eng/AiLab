# 진행 상황 종합 (2026-09-04 기준, 로컬 세션 갱신)

이 세션에서 다룬 두 갈래 작업 — ① 네이버 블로그 「돈의 구조를 읽고 만드는 삶」 구축, ② 투자 카페 유입용 원고 — 의 현재 상태를 정리한다.

## 문서 지도

| 파일 | 내용 | 상태 |
|---|---|---|
| `docs/blog-plan.md` | 블로그 전체 계획. 실사 결과(§7)로 최신화됨 | 유지보수 중 |
| `docs/category-review.md` | 블로그 카테고리 이름·구조 검토, 통합안 | 확정안 대기 (실제 글 수 필요) |
| `docs/assets/blog-cover-pc.png`, `blog-cover-mobile.png` | 재제작된 블로그 대문 이미지 2종 | 완성, 적용 대기 |
| `docs/assets/blog-cover-build.py` | 대문 이미지 재생성 스크립트 | 완성 |
| `docs/cafe-post-pattern.md` | 외부 레퍼런스 글에서 뽑은 8단 글쓰기 패턴 + 체크리스트 | 완성 |
| `docs/cafe-post-01.md` | 카페 1편 — 종목ELS 소개 | **확정** |
| `docs/cafe-post-02.md` | 카페 2편 — 개별 상품 고르는 기준 | **확정** |
| `docs/cafe-post-03.md` | 다른 카페용 3편 — ELS 175개 관리 도구 소개 | **확정** (스크린샷 3장 확보·마스킹 완료) |
| `docs/assets/els-weekly-screening.png`, `els-portfolio.png`, `els-calendar.png` | 카페 3편 삽입용 화면 3장 | 완성 (금액·계정명 마스킹) |

## ① 블로그 구축 — 현재 막힌 지점

1. **카테고리 정리**: 26개(상위 6 + 하위 19 + 보관함) vs 게시물 25개. `category-review.md`에 통합안(15개)까지 만들어뒀으나, **카테고리별 실제 글 수**를 몰라서 확정하지 못함. 관리 화면에서 숫자 확인 필요.
2. **대문 이미지 적용**: 이미지는 완성됐으나 아직 블로그에 업로드되지 않음. 기존 `ELS RADAR` 배너를 이 이미지로 교체할지, 별도 위치로 옮길지는 `blog-plan.md` §2 "안 A"로 결정된 상태.
3. **최상단 공지 교체**: 현재 최상단이 부동산 스크랩 공지(2023-04-27). 「돈의 구조를 읽고 만드는 삶을 시작합니다」로 교체 필요.
4. **네트워크 제약(해소)**: 클라우드 세션에서는 `naver.com`, `elsrader.site` 접속이 차단됐으나(egress 정책), 로컬 세션에서는 둘 다 접속됨. 다만 네이버 블로그 관리 화면은 로그인이 필요하므로, 사용자가 브라우저에서 직접 로그인한 뒤 세션을 넘겨받는 방식으로 진행해야 함.

## ② 카페 원고 — 현재 상태

1. **카페 1·2편**: 완성, 게시 가능한 상태. 게시 전 검토 메모(각 파일 하단)만 한 번 확인.
2. **카페 3편**(다른 카페용): **완성.** 2026-09-04 로컬 세션에서 아래를 처리해 막힌 것이 모두 해소됨.
   - 스크린샷 3장을 로컬 Playwright로 직접 캡처해 `docs/assets/`에 추가. 원고의 삽입 표시를 실제 이미지 참조로 교체.
   - 백만 원 단위 이상 모든 금액을 `•••,•••,•••` 로, 우상단 계정명 `admin`을 `•••••` 로 치환. 비율·건수는 유지.
   - 1·2편을 안 본 독자용이므로 "지난 글에서" 참조 4곳을 전부 자체 완결 서술로 다시 씀.
3. 랜딩 히어로 화면(베타 배지 + CTA 버튼)은 광고성으로 보여 카페 게시판 규정(홍보 금지) 위반 소지가 있어 **미사용**.
4. 게시처는 네이버 카페 **「은퇴후 50년」**으로 확정. 은퇴·노후 준비층에 맞춰 도입부(원금 비보장 고지 포함), 상환 캘린더 단락(현금흐름 달력 관점), 마무리 세 곳을 조정함. 화자는 은퇴자로 꾸미지 않고 ELS 실투자자 그대로 둠.
5. 주간 청약 화면의 발행사명·상품번호는 **가리지 않고 그대로 게시**하기로 확정. 다만 본문에서 특정 발행사를 지목하지는 않음.

## 세션 핸드오프

- 클라우드 세션(브랜치 `claude/chatgpt-link-summary-x6ik4l`, 세션 ID `session_018vFu1s1Ms2cjJYMD6zvpnd`)에서 작업하다가, 2026-09-04 **로컬 세션이 같은 브랜치를 클론해 이어받음**. 텔레포트는 쓰지 않음.
- 로컬에서는 `naver.com`, `elsrader.site` 모두 접속됨(클라우드의 egress 차단 해소). 로그인이 필요한 화면은 헤드풀 브라우저를 띄워 사용자가 직접 로그인한 뒤, 세션 프로필을 재사용해 캡처하는 방식으로 처리함. 캡처 스크립트는 스크래치패드에 있고 리포지터리에는 두지 않음.

## 다음에 할 일 (우선순위순)

1. 카페 3편을 「은퇴후 50년」에 게시 (원고·이미지 준비 완료)
2. 카페 1편 게시 → 반응 보고 2편 게시 여부 결정
3. **블로그 관련 작업(카테고리 정리, 대문 이미지 적용, 공지 교체)은 사용자가 직접 처리하기로 함.** 이 리포지터리의 블로그 문서는 참고용으로만 유지.
