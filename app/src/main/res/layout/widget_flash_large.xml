<?xml version="1.0" encoding="utf-8"?>
<!-- Enlarged form, used once the widget is stretched to about two cells or more.
     Same view ids as widget_flash so both are driven by the same RemoteViews calls. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg_off"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="12dp">

    <ImageView
        android:id="@+id/widget_icon"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:contentDescription="@null"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_flash_amber" />

    <TextView
        android:id="@+id/widget_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:text="@string/state_off"
        android:textColor="@color/off_foreground"
        android:textSize="26sp"
        android:textStyle="bold" />

</LinearLayout>
